# Hadolint конфигурация
# Игнорируем предупреждения, которые не критичны для нашего проекта

ignored:
  - DL3013  # Pin versions in pip (uv обновляется часто, хотим последнюю)
  - DL3008  # Pin versions in apt (curl обновляется через base image)
